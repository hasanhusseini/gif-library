{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the Phase 0 application shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}

